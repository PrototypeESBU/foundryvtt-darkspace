<div class="DS-box {{class}}">
	<div class="header {{header-class}}">
		<label>
			{{header-label}}
		</label>
		<span>
			{{#> ds-box-header-span}}{{header-span}}{{/ds-box-header-span}}
		</span>
	</div>
	<div class="content {{content-class}}">
		{{> @partial-block }}
	</div>
</div>
